/**
 * Единый источник правил сворачивания символов для поиска.
 * Используется ВЕЗДЕ, где нужна нормализация названия:
 *   • lib/catalog/normalize.ts (рантайм-нормализация запроса);
 *   • app/api/catalog/text-search/route.ts (SQL translate в WHERE);
 *   • scripts/enable-fuzzy-search.mjs (выражение функционального индекса).
 *
 * Эти три места должны использовать ОДНИ И ТЕ ЖЕ строки, иначе функциональный
 * GIN-индекс перестанет применяться. Поэтому константы живут здесь в одном .mjs,
 * который импортируют и TS-, и .mjs-файлы — расхождение становится невозможным.
 *
 * ё → е и латинские буквы-двойники (раскладка не переключена) → кириллица.
 * Порядок символов в FROM соответствует порядку в TO.
 */
export const FOLD_FROM = "ёabcehkmoptxy";
export const FOLD_TO = "еавсенкмортху";
